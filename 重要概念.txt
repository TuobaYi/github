3个对象
    git对象
        存储hash和内容键值对的对象（没有存储文件名！！！）
    树对象
        当我们往数据库塞git对象的时候 没有生成树对象
            什么时候 git帮我们把树对象塞进git数据库？
    提交对象
        对树对象的一个包装
        每一个提交对象都是：提交时的项目快照（树对象） + 提交信息


3个区域
    工作目录
        沙箱，随意操作
    暂存区
        生成树对象（git 会在某一时刻生成树对象）
    本地库
        存git对象 树对象 提交对象